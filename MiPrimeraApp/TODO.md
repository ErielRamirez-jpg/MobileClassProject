# TODO - Arreglo del programa

- [x] Paso 1: Corregir tipado de Redux hooks en `src/store/hooks.ts` (evitar `useDispatch.withTypes` / `useSelector.withTypes`).
- [ ] Paso 2: Revisar pantallas que usan `useAppSelector/useAppDispatch` y corregir cualquier error de tipado/props que aparezca.
- [ ] Paso 3: Mejorar `LanguageContext` para que `clearLanguage` restaure `i18n.locale`/estado de forma consistente.
- [ ] Paso 4: Ejecutar build/compilación para detectar errores restantes y corregir hasta que el programa compile y funcione.

